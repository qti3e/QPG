class Source {
  constructor(source) {
    this.source = source;
  }

  array(fn) {
  }
}

function SOURCE(source) {
  const ret = { name: "SOURCE", parts: [] };
  parts.push(source.array(import_declaration));
  parts.push(source.array(declaration));
  return ret;
}

function import_declaration(source) {
}
